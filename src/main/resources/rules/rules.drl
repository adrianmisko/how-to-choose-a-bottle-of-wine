package sample;
import java.sql.SQLOutput

global Controller controller;

rule "Who"
    when

    then
        Question question = new Question("Who is it for?");
        question.getPossibleAnswers().add("Personal use");
        question.getPossibleAnswers().add("Someone else");
        controller.getNewQuestion(question);
end

rule "Personal use"
    when
        a: Answer(text == "Personal use")       //to nie przejdzie
    then
        Question question = new Question("Are you at home?");
        question.getPossibleAnswers().add("Yes");
        question.getPossibleAnswers().add("No");
        controller.getNewQuestion(question);
end


rule "Someone else"
    when
        a: Answer(text == "Someone else")
    then
        Question question = new Question("Are you at social gathering?");
        question.getPossibleAnswers().add("Yes");
        question.getPossibleAnswers().add("No");
        controller.getNewQuestion(question);
end


rule "At home"
    when
        a: Answer(text == "Yes")
    then
        Question question = new Question("dupa");
        controller.getNewQuestion(question);
end